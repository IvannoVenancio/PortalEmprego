<nav class="pagina-home">
    <header class="pagina-home">
        <a href="/home" class="logo">Digma Works</a>
    </header>
    {{#if isTeladelogin}}
    <header class="pagina-home">
        <a href="/home" class="logo">Digma Works</a>
    </header>
    {{/if}}

    <!-- Condicional para exibir links diferentes -->
    {{#if isPaginainicial}}
    <header class="pagina-home">
        <a href="/home" class="logo">Digma Works</a>
    </header>
    </div>
    {{/if}}


    <!-- Condicional para exibir links diferentes -->
    {{#if isPaginaRecrutadores}}
    <header class="pagina-home">
        <a href="/home" class="logo">Digma Works</a>
    </header>

    </div>
    {{/if}}

    {{#if ishome}}
    <header class="pagina-home">
        <a href="/home" class="logo">Digma Works</a>
        <div class="buttons">
            <a href="/login" class="login-btn">Entrar</a>
            <a href="/cadastro" class="register-btn">Cadastrar-se</a>
        </div>
    </header>

    {{/if}}

</nav>