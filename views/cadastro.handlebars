
  <link rel="stylesheet" href="/CSS/Style.css"/>

<body class="container-cadastro">
  
<h2 class="titulo-cadastro">CRIE SUA CONTA AGORA OPORTUNIDADES ESPERAM POR VOCÊS!</h2>
  <div class="container-registro">
    <h2>Cadastrar-se</h2>
    <form  action="/create_user" method="post">
    
      <label for="email">Email:</label>
      <input type="email" id="email" name="email" required /><br>
      <label for="password">Senha:</label>
      <input type="password" id="password" name="password" required /><br>

        <select name="tipo_usuario" id="">
        <option value="">Selecione...</option>
        {{#each TipoUsuario}}
        <option value="{{id}}">{{tipo}}</option>
        {{/each}}
    </select> <br>

      <button type="submit">Cadastrar</button>
    </form>
    
    <p>Já tem uma conta? <a href="/login">Entrar</a></p>
  </div>

  <div class="buttons">
      <a href="/Escolha" class="login-btn">Escolha</a>
      <a href="/tipo_usuario">User Type</a>
    </div>

    <table>
    <tr>
        <td>#</td>
        <td>Email</td>
    </tr>
    {{#each user  }}
    <tr>
        <td>{{id}}</td>
        <td>{{email}}</td>
        
    </tr>
    {{/each}}
</table>
</body>
</html>
